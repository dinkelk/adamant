---
description: GNATprove configuration for the byte array utilities. The target is gold, the copy contents, the status of every operation and the bytes each one leaves untouched, on top of absence of runtime errors.
level: 2
mode: "gold"
